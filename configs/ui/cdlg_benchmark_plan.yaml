# This file owns only the explicit execution queue.
# Preset payloads remain in experiment_ui_presets.json.
# Remove or reorder entries before running the benchmark.
runs:
  - preset: EOPKG-drift
  - preset: EOPKG-WI-drift
  - preset: GATv2+Mask-drift
  - preset: GATv2-drift
  - preset: LSTM-drift