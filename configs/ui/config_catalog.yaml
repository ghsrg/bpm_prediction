version: 1
fields:
  data.dataset_label:
    path: data.dataset_label
    section: data
    label: Мітка датасету
    description: Human-readable dataset label for logs and tracking.
    affects: Input loading and process identity.
    default: ''
    enum:
    - bpi2012
    - bpi2012_stage3_4_xes
    - bpi2012_stage4_2_xes_stat
    - bpi2020Permit1
    - camunda_b2bcontracts
    - camunda_b2bcontracts_stage4_2_stat
    - camunda_procurement
    - camunda_procurement_sql
    - camunda_procurement_stage3_4_sql
    - camunda_sync_files_file
    - camunda_sync_files_neo4j
    - camunda_sync_sql_file
    - camunda_sync_sql_neo4j
    - synthetic_drift_4tu
    - xes_sync_dir_file
    - xes_sync_dir_neo4j
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: source
      priority: 1
      order: 2
  data.dataset_name:
    path: data.dataset_name
    section: data
    label: Канонічна назва датасету
    description: Canonical process/dataset identifier used for structure lookup and
      run profile.
    affects: Input loading and process identity.
    default: ''
    enum:
    - B2BContracts_ApproveProject
    - BPI_Challenge_2012
    - bpi2012
    - camunda_sync
    - procurement
    - xes_sync
    required_in_modes:
    - train
    - eval_drift
    - eval_cross_dataset
    - sync-topology
    - sync-stats
    - sync-stats-backfill
    required_when: {}
    ui:
      tab: input
      group: source
      priority: 1
      order: 1
  data.log_path:
    path: data.log_path
    section: data
    label: Шлях до XES логу
    description: Path to XES file (or XES directory in sync scenarios).
    affects: Input loading and process identity.
    default: ''
    enum: []
    required_in_modes: []
    required_when:
      mapping.adapter: xes
    ui:
      tab: input
      group: source
      priority: 1
      order: 4
  experiment.cache_policy:
    path: experiment.cache_policy
    section: experiment
    label: Політика кешування графів
    description: Caching policy for graph build path (off/dto/full).
    affects: Build-graph performance and repeated repository lookups.
    default: full
    enum:
    - false
    - dto
    - full
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 14
  experiment.cache_dir:
    path: experiment.cache_dir
    section: experiment
    label: experiment.cache_dir
    description: Disk path for persistent structure cache when experiment.cache_policy=full.
    affects: Reuse of compiled structural tensors between runs.
    default: .cache/dynamic_graph_builder
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 20
      order: 220
  experiment.graph_dataset_cache_policy:
    path: experiment.graph_dataset_cache_policy
    section: experiment
    label: Дозволені операції з кешем графів
    description: Policy for disk cache of built graph datasets (off/read/write/full).
    affects: Reuse of prepared train/validation/test graphs between runs.
    default: 'off'
    enum:
    - 'off'
    - read
    - write
    - full
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 15
  experiment.graph_dataset_cache_dir:
    path: experiment.graph_dataset_cache_dir
    section: experiment
    label: місце збереження кеша
    description: Disk path for persistent cache of prepared graph datasets.
    affects: Location of reusable train/validation/test graph artifacts.
    default: .cache/graph_datasets
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 16
  experiment.graph_dataset_disk_spill_enabled:
    path: experiment.graph_dataset_disk_spill_enabled
    section: experiment
    label: experiment.graph_dataset_disk_spill_enabled
    description: Enable sharded disk spill for build_graph to keep RAM bounded.
    affects: Memory usage during graph dataset build and cache write path.
    default: 'false'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 17
  experiment.graph_dataset_shard_size:
    path: experiment.graph_dataset_shard_size
    section: experiment
    label: experiment.graph_dataset_shard_size
    description: Target number of graphs per on-disk shard when spill mode is enabled.
    affects: Balance between disk IO overhead and peak RAM during build_graph.
    default: 2000
    enum: []
    required_in_modes: []
    required_when:
      experiment.graph_dataset_disk_spill_enabled: true
    ui:
      tab: general
      group: advanced
      priority: 25
      order: 225
  experiment.max_ram_gb:
    path: experiment.max_ram_gb
    section: experiment
    label: experiment.max_ram_gb
    description: Soft RSS limit for build_graph spill flushes; 0 disables RAM guard.
    affects: Upper bound of process memory during graph dataset preparation.
    default: 0.0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 25
      order: 226
  experiment.drift_window_size:
    path: experiment.drift_window_size
    section: experiment
    label: 'Експеримент: Drift window size'
    description: Window size (in traces) for drift evaluation.
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum:
    - '1000'
    - '500'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  experiment.drift_window_sliding:
    path: experiment.drift_window_sliding
    section: experiment
    label: 'Експеримент: Drift window sliding'
    description: Window sliding step for drift evaluation.
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum:
    - '100'
    - '200'
    - '50'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  experiment.fraction:
    path: experiment.fraction
    section: experiment
    label: Частка даних (fraction)
    description: Trace fraction after cascade filtering (0..1].
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum:
    - '0.1'
    - '1'
    - '1.0'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 4
  experiment.fraction_strategy:
    path: experiment.fraction_strategy
    section: experiment
    label: Fraction strategy
    description: Data fraction strategy. temporal preserves chronological sampling; versioned samples independently per process version.
    affects: Data split and experiment validity.
    default: temporal
    enum:
    - temporal
    - versioned
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 5
  experiment.load_checkpoint:
    path: experiment.load_checkpoint
    section: experiment
    label: 'Експеримент: Завантаження checkpoint'
    description: Path to checkpoint used in eval_* modes or for manual resume.
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum:
    - checkpoints/MVP1_Baseline_train_05_bpi2012_GATv2_best.pth
    - checkpoints/MVP1_Baseline_train_bpi2012_GATv2_best.pth
    - checkpoints/MVP1_train_DataDrift_1_synthetic_drift_4tu_GATv2_best.pth
    - checkpoints/MVP2_5_Stage3_1_Baseline_Files_camunda_procurement_GATv2_best.pth
    - checkpoints/MVP2_5_Stage3_1_EOPKG_Files_camunda_procurement_GATv2_best.pth
    - checkpoints/MVP2_EOPKG_Attention_train_05_bpi2012_GATv2_best.pth
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  experiment.mode:
    path: experiment.mode
    section: experiment
    label: Режим запуску
    description: Pipeline run mode (train/eval/sync commands).
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum:
    - train
    - eval_drift
    - eval_cross_dataset
    - sync-topology
    - sync-stats
    - sync-stats-backfill
    required_in_modes:
    - train
    - eval_drift
    - eval_cross_dataset
    - sync-topology
    - sync-stats
    - sync-stats-backfill
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 1
  experiment.name:
    path: experiment.name
    section: experiment
    label: Назва запуску
    description: Concrete run name.
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum: []
    required_in_modes:
    - train
    - eval_drift
    - eval_cross_dataset
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 3
  experiment.on_missing_asof_snapshot:
    path: experiment.on_missing_asof_snapshot
    section: experiment
    label: Політика при відсутньому as-of snapshot
    description: Runtime policy for strict_asof when no eligible snapshot is found (disable_stats/use_base/raise).
    affects: Temporal safety and fallback behavior for stats tensors.
    default: disable_stats
    enum:
    - disable_stats
    - use_base
    - raise
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  experiment.project:
    path: experiment.project
    section: experiment
    label: Проєкт експерименту
    description: Tracking project/experiment name.
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum:
    - bpm_prediction_mvp1
    - bpm_prediction_mvp2
    - bpm_prediction_mvp2_5
    required_in_modes:
    - train
    - eval_drift
    - eval_cross_dataset
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 2
  experiment.split_ratio:
    path: experiment.split_ratio
    section: experiment
    label: Співвідношення train/val/test
    description: Final [train,val,test] split ratio.
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 8
  experiment.split_strategy:
    path: experiment.split_strategy
    section: experiment
    label: Стратегія розбиття
    description: Data split strategy. Current canonical value for MVP2.5 is temporal.
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum:
    - temporal
    - none
    - versioned
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 7
  experiment.stats_time_policy:
    path: experiment.stats_time_policy
    section: experiment
    label: Політика часу статистики
    description: 'Stats snapshot policy: latest or strict_asof.'
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  experiment.train_ratio:
    path: experiment.train_ratio
    section: experiment
    label: Частка train
    description: Train-cut ratio in cascade preparation.
    affects: Execution route, data split, and evaluation mode behavior.
    default: ''
    enum:
    - '0'
    - '0.01'
    - '0.4'
    - '0.5'
    - '0.7'
    - '1.0'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 6
  experiment.version_scope_policy:
    path: experiment.version_scope_policy
    section: experiment
    label: Version scope policy
    description: Version leakage control for versioned fraction/split. all uses all selected versions; train_cut restricts train to versions known before train_ratio cut and keeps future versions for eval tail.
    affects: Data split and future-version leakage control.
    default: all
    enum:
    - all
    - train_cut
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 9
  experiment.structural_mode:
    path: experiment.structural_mode
    section: experiment
    label: structural_mode
    description: Enable or disable EOPKG structural attention branch while keeping observed branch unchanged.
    affects: Clean structural ablation (on versus off) for EOPKG models.
    default: 'true'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 30
      order: 230
  experiment.statistic_enabled:
    path: experiment.statistic_enabled
    section: experiment
    label: statistic_enabled
    description: Convenience toggle mapped to mapping.graph_feature_mapping.enabled.
    affects: Whether structural statistics tensors are produced and consumed.
    default: 'false'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 30
      order: 231
  experiment.mask_guided_enabled:
    path: experiment.mask_guided_enabled
    section: experiment
    label: mask_guided_enabled
    description: Convenience toggle mapped to training.mask_guided_enabled.
    affects: Enables mask-guided training and inference penalties.
    default: 'false'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 30
      order: 232
  experiment.retrain:
    path: experiment.retrain
    section: experiment
    label: retrain
    description: Convenience toggle mapped to training.retrain.
    affects: Force training from scratch instead of resume.
    default: 'false'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 30
      order: 233
  mapping.adapter:
    path: mapping.adapter
    section: mapping
    label: Адаптер даних
    description: Data/structure mapping parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum:
    - xes
    - camunda
    required_in_modes:
    - train
    - eval_drift
    - eval_cross_dataset
    - sync-topology
    - sync-stats
    - sync-stats-backfill
    required_when: {}
    ui:
      tab: input
      group: source
      priority: 1
      order: 3
  mapping.camunda_adapter.lookback_hours:
    path: mapping.camunda_adapter.lookback_hours
    section: mapping
    label: 'Мапінг: Lookback hours'
    description: Camunda process/tenant/time filtering parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.process_filters:
    path: mapping.camunda_adapter.process_filters
    section: mapping
    label: 'Мапінг: Процес фільтри'
    description: Camunda process/tenant/time filtering parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.process_name:
    path: mapping.camunda_adapter.process_name
    section: mapping
    label: 'Мапінг: Процес назва'
    description: Camunda process/tenant/time filtering parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum:
    - B2BContracts_ApproveProject
    - procurement
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.runtime.connection_profile:
    path: mapping.camunda_adapter.runtime.connection_profile
    section: mapping
    label: 'Мапінг: Підключення профіль'
    description: Camunda runtime source/connection/filter parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.runtime.connections_file:
    path: mapping.camunda_adapter.runtime.connections_file
    section: mapping
    label: 'Мапінг: Підключення файл'
    description: Camunda runtime source/connection/filter parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.runtime.export_dir:
    path: mapping.camunda_adapter.runtime.export_dir
    section: mapping
    label: 'Мапінг: Export dir'
    description: Camunda runtime source/connection/filter parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.runtime.history_cleanup_aware:
    path: mapping.camunda_adapter.runtime.history_cleanup_aware
    section: mapping
    label: 'Мапінг: History cleanup aware'
    description: Camunda runtime source/connection/filter parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.runtime.legacy_removal_time_policy:
    path: mapping.camunda_adapter.runtime.legacy_removal_time_policy
    section: mapping
    label: 'Мапінг: Legacy removal time політика'
    description: Camunda runtime source/connection/filter parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.runtime.mssql.connection_profile:
    path: mapping.camunda_adapter.runtime.mssql.connection_profile
    section: mapping
    label: 'Мапінг: Підключення профіль'
    description: Camunda runtime source/connection/filter parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.runtime.on_missing_removal_time:
    path: mapping.camunda_adapter.runtime.on_missing_removal_time
    section: mapping
    label: 'Мапінг: On missing removal time'
    description: Camunda runtime source/connection/filter parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.runtime.runtime_source:
    path: mapping.camunda_adapter.runtime.runtime_source
    section: mapping
    label: 'Мапінг: Runtime джерело'
    description: Camunda runtime source/connection/filter parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum:
    - files
    - mssql
    required_in_modes: []
    required_when:
      mapping.adapter: camunda
    ui:
      tab: input
      group: source
      priority: 2
      order: 10
  mapping.camunda_adapter.runtime.sql_dir:
    path: mapping.camunda_adapter.runtime.sql_dir
    section: mapping
    label: 'Мапінг: Sql dir'
    description: Camunda runtime source/connection/filter parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.since:
    path: mapping.camunda_adapter.since
    section: mapping
    label: 'Мапінг: Since'
    description: Camunda process/tenant/time filtering parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.structure.bpmn_source:
    path: mapping.camunda_adapter.structure.bpmn_source
    section: mapping
    label: 'Мапінг: Bpmn джерело'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum:
    - files
    - mssql
    required_in_modes: []
    required_when:
      mapping.adapter: camunda
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 2
  mapping.camunda_adapter.structure.call_activity.inference_fallback_strategy:
    path: mapping.camunda_adapter.structure.call_activity.inference_fallback_strategy
    section: mapping
    label: 'Мапінг: Inference fallback стратегія'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 50
  mapping.camunda_adapter.structure.connection_profile:
    path: mapping.camunda_adapter.structure.connection_profile
    section: mapping
    label: 'Мапінг: Підключення профіль'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 6
  mapping.camunda_adapter.structure.connections_file:
    path: mapping.camunda_adapter.structure.connections_file
    section: mapping
    label: 'Мапінг: Підключення файл'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 7
  mapping.camunda_adapter.structure.files.bpmn_dir:
    path: mapping.camunda_adapter.structure.files.bpmn_dir
    section: mapping
    label: 'Мапінг: Bpmn dir'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 5
  mapping.camunda_adapter.structure.files.catalog_file:
    path: mapping.camunda_adapter.structure.files.catalog_file
    section: mapping
    label: 'Мапінг: Catalog файл'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 4
  mapping.camunda_adapter.structure.files.export_dir:
    path: mapping.camunda_adapter.structure.files.export_dir
    section: mapping
    label: 'Мапінг: Export dir'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 3
  mapping.camunda_adapter.structure.mssql.connection_profile:
    path: mapping.camunda_adapter.structure.mssql.connection_profile
    section: mapping
    label: 'Мапінг: Підключення профіль'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 50
  mapping.camunda_adapter.structure.parser_mode:
    path: mapping.camunda_adapter.structure.parser_mode
    section: mapping
    label: 'Мапінг: Parser режим'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 9
  mapping.camunda_adapter.structure.source:
    path: mapping.camunda_adapter.structure.source
    section: mapping
    label: 'Мапінг: Джерело'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum:
    - bpmn
    - logs
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 1
  mapping.camunda_adapter.structure.sql_dir:
    path: mapping.camunda_adapter.structure.sql_dir
    section: mapping
    label: 'Мапінг: Sql dir'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 8
  mapping.camunda_adapter.structure.structure_from_logs:
    path: mapping.camunda_adapter.structure.structure_from_logs
    section: mapping
    label: 'Мапінг: Структура from logs'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 50
  mapping.camunda_adapter.structure.subprocess_mode:
    path: mapping.camunda_adapter.structure.subprocess_mode
    section: mapping
    label: 'Мапінг: Subprocess режим'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 10
  mapping.camunda_adapter.structure.version_key_format:
    path: mapping.camunda_adapter.structure.version_key_format
    section: mapping
    label: 'Мапінг: Версія ключ format'
    description: Camunda BPMN structure ingestion parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 50
  mapping.camunda_adapter.subprocess_graph_mode:
    path: mapping.camunda_adapter.subprocess_graph_mode
    section: mapping
    label: 'Мапінг: Subprocess graph режим'
    description: Camunda process/tenant/time filtering parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: structure
      priority: 2
      order: 11
  mapping.camunda_adapter.tenant_filters:
    path: mapping.camunda_adapter.tenant_filters
    section: mapping
    label: 'Мапінг: Тенант фільтри'
    description: Camunda process/tenant/time filtering parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.tenant_id:
    path: mapping.camunda_adapter.tenant_id
    section: mapping
    label: 'Мапінг: Тенант id'
    description: Camunda process/tenant/time filtering parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.until:
    path: mapping.camunda_adapter.until
    section: mapping
    label: 'Мапінг: Until'
    description: Camunda process/tenant/time filtering parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.camunda_adapter.version_key:
    path: mapping.camunda_adapter.version_key
    section: mapping
    label: 'Мапінг: Версія ключ'
    description: Camunda process/tenant/time filtering parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum:
    - ''
    - v5
    - v6
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.knowledge_graph.backend:
    path: mapping.knowledge_graph.backend
    section: mapping
    label: Бекенд репозиторію знань
    description: Repository backend for structure/snapshot artifacts.
    affects: Persistence mode and runtime loading path for structure/statistics.
    default: file
    enum:
    - in_memory
    - file
    - neo4j
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: backend
      priority: 1
      order: 1
  mapping.knowledge_graph.ingest_split:
    path: mapping.knowledge_graph.ingest_split
    section: mapping
    label: 'Мапінг: Ingest розбиття'
    description: Knowledge graph backend/storage/connectivity parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum:
    - full
    - train
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: backend
      priority: 1
      order: 7
  mapping.knowledge_graph.neo4j.connection_profile:
    path: mapping.knowledge_graph.neo4j.connection_profile
    section: mapping
    label: 'Мапінг: Підключення профіль'
    description: Knowledge graph backend/storage/connectivity parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: backend
      priority: 1
      order: 3
  mapping.knowledge_graph.neo4j.connections_file:
    path: mapping.knowledge_graph.neo4j.connections_file
    section: mapping
    label: 'Мапінг: Підключення файл'
    description: Knowledge graph backend/storage/connectivity parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: backend
      priority: 1
      order: 4
  mapping.knowledge_graph.neo4j.verify_connectivity:
    path: mapping.knowledge_graph.neo4j.verify_connectivity
    section: mapping
    label: 'Мапінг: Перевірка connectivity'
    description: Knowledge graph backend/storage/connectivity parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: backend
      priority: 1
      order: 5
  mapping.knowledge_graph.path:
    path: mapping.knowledge_graph.path
    section: mapping
    label: 'Мапінг: Шлях'
    description: Knowledge graph backend/storage/connectivity parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: backend
      priority: 1
      order: 6
  mapping.knowledge_graph.strict_load:
    path: mapping.knowledge_graph.strict_load
    section: mapping
    label: Суворе завантаження структури
    description: Knowledge graph backend/storage/connectivity parameter.
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: backend
      priority: 1
      order: 2
  mapping.graph_feature_mapping.enabled:
    path: mapping.graph_feature_mapping.enabled
    section: mapping
    label: Графове stats-мапінг увімкнено
    description: Enables canonical stats_index -> tensor mapping for structural branch.
    affects: Whether struct_x/structural stats features are produced in runtime.
    default: false
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: legacy
      group: deprecated
      priority: 999
      order: 9999
  mapping.graph_feature_mapping.topology_projection.gateway_mode:
    path: mapping.graph_feature_mapping.topology_projection.gateway_mode
    section: mapping
    label: gateways
    description: Controls whether BPMN gateway nodes are preserved or collapsed into task-to-task projection for GNN/mask runtime.
    affects: Structural edge tensor and allowed_target_mask construction.
    default: collapse_for_prediction
    enum:
    - preserve
    - collapse_for_prediction
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: graph_mapping
      priority: 2
      order: 1
  mapping.graph_feature_mapping.topology_projection.diagnostics_enabled:
    path: mapping.graph_feature_mapping.topology_projection.diagnostics_enabled
    section: mapping
    label: Projection diagnostics enabled
    description: Enables post-projection topology alignment diagnostics for structural tensors and allowed_target_mask.
    affects: Runtime diagnostics, forward stats, and strict projection alignment checks.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: graph_mapping
      priority: 2
      order: 2
  mapping.graph_feature_mapping.topology_projection.on_fail:
    path: mapping.graph_feature_mapping.topology_projection.on_fail
    section: mapping
    label: Projection alignment on_fail
    description: Action when post-projection topology alignment cannot be proven.
    affects: Runtime fail-fast behavior for collapse_for_prediction and structural tensor use.
    default: warn
    enum:
    - warn
    - raise
    - disable_struct
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: graph_mapping
      priority: 2
      order: 3
  mapping.graph_feature_mapping.stats_quality_gate.enabled:
    path: mapping.graph_feature_mapping.stats_quality_gate.enabled
    section: mapping
    label: Consumer stats quality gate enabled
    description: Enables consumer-side stats quality gate in DynamicGraphBuilder.
    affects: Runtime decision to allow/suppress degraded stats tensors.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: graph_mapping
      priority: 2
      order: 4
  mapping.graph_feature_mapping.stats_quality_gate.on_fail:
    path: mapping.graph_feature_mapping.stats_quality_gate.on_fail
    section: mapping
    label: Consumer stats quality gate on_fail
    description: Action on consumer quality gate fail.
    affects: Runtime fallback policy for stats tensors.
    default: ignore_with_warning
    enum:
    - ignore_with_warning
    - allow_with_warning
    - raise
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: graph_mapping
      priority: 2
      order: 3
  mapping.graph_feature_mapping.stats_quality_gate.warn_on_fail:
    path: mapping.graph_feature_mapping.stats_quality_gate.warn_on_fail
    section: mapping
    label: Consumer stats quality gate warn_on_fail
    description: Emit warning when consumer quality gate rejects or downgrades stats payload.
    affects: Runtime diagnostics verbosity.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: graph_mapping
      priority: 2
      order: 4
  mapping.graph_feature_mapping.stats_quality_gate.zero_dominant_threshold:
    path: mapping.graph_feature_mapping.stats_quality_gate.zero_dominant_threshold
    section: mapping
    label: Consumer zero-dominant threshold
    description: Threshold above which stats snapshot is considered zero-dominant.
    affects: Runtime quality gate acceptance.
    default: 0.95
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: graph_mapping
      priority: 2
      order: 5
  mapping.graph_feature_mapping.stats_quality_gate.min_non_zero_ratio_overall:
    path: mapping.graph_feature_mapping.stats_quality_gate.min_non_zero_ratio_overall
    section: mapping
    label: Consumer min non-zero ratio overall
    description: Minimal overall non-zero ratio required by consumer gate.
    affects: Runtime quality gate acceptance.
    default: 0.0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: graph_mapping
      priority: 2
      order: 6
  mapping.graph_feature_mapping.stats_quality_gate.min_history_coverage_percent:
    path: mapping.graph_feature_mapping.stats_quality_gate.min_history_coverage_percent
    section: mapping
    label: Consumer min history coverage percent
    description: Minimal history coverage required by consumer gate.
    affects: Runtime quality gate acceptance.
    default: 0.0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: graph_mapping
      priority: 2
      order: 7
  mapping.xes_adapter.activity_key:
    path: mapping.xes_adapter.activity_key
    section: mapping
    label: 'Мапінг: Activity ключ'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.xes_adapter.case_id_key:
    path: mapping.xes_adapter.case_id_key
    section: mapping
    label: 'Мапінг: Case id ключ'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.xes_adapter.complete_transitions:
    path: mapping.xes_adapter.complete_transitions
    section: mapping
    label: 'Мапінг: Complete transitions'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.xes_adapter.start_transitions:
    path: mapping.xes_adapter.start_transitions
    section: mapping
    label: 'Mapping: Start transitions'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: core
      priority: 1
      order: 4
  mapping.xes_adapter.lifecycle_key:
    path: mapping.xes_adapter.lifecycle_key
    section: mapping
    label: 'Мапінг: Lifecycle ключ'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.xes_adapter.pairing_strategy:
    path: mapping.xes_adapter.pairing_strategy
    section: mapping
    label: 'Мапінг: Pairing стратегія'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.xes_adapter.resource_key:
    path: mapping.xes_adapter.resource_key
    section: mapping
    label: 'Мапінг: Resource ключ'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.xes_adapter.timestamp_key:
    path: mapping.xes_adapter.timestamp_key
    section: mapping
    label: 'Мапінг: Timestamp ключ'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.xes_adapter.use_classifier:
    path: mapping.xes_adapter.use_classifier
    section: mapping
    label: 'Мапінг: Use classifier'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  mapping.xes_adapter.version_key:
    path: mapping.xes_adapter.version_key
    section: mapping
    label: 'Мапінг: Версія ключ'
    description: XES parsing/mapping parameter (case/activity/time/version/lifecycle
      alignment).
    affects: Ingestion correctness and structure/stats availability.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  model.cross_attention_heads:
    path: model.cross_attention_heads
    section: model
    label: 'Модель: Cross attention голови'
    description: Model architecture/hyperparameter.
    affects: Capacity, compute footprint, and generalization behavior.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
  model.fusion_mode:
    path: model.fusion_mode
    section: model
    label: 'Модель: Fusion mode'
    description: ClassMeanAttention keeps old Attention semantics; ClassMeanConcat keeps old Concat/struct_pool_concat semantics; ClassAwareStructuralScoring adds per-class structural logits to observed logits; TopologyStateEncoder projects observed prefix state onto topology nodes before structural message passing and pools node logits to classes; TopologyStateGraphEncoder projects observed prefix state onto topology nodes, performs topology message passing, then classifies a graph-level topology context; StructuralPriorEncoder uses etalon-like representation-level fusion with observed context plus pooled structural context before classifier; StructXAttn uses token-level structural cross-attention from observed IG nodes to structural nodes before pooling.
    affects: How structural context is combined with observed branch in EOPKG mode.
    default: ClassMeanAttention
    enum:
    - ClassMeanAttention
    - ClassMeanConcat
    - ClassAwareStructuralScoring
    - TopologyStateEncoder
    - TopologyStateGraphEncoder
    - StructuralPriorEncoder
    - StructXAttn
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
  model.structural_score_mode:
    path: model.structural_score_mode
    section: model
    label: 'Model: Structural score mode'
    description: Scoring function used by ClassAwareStructuralScoring before node-to-class late pooling.
    affects: Per-class structural logit signal for topology-aware ranking.
    default: bilinear_with_prior
    enum:
    - bilinear_with_prior
    - cosine
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: ClassAwareStructuralScoring
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1001
  model.structural_logit_scale_init:
    path: model.structural_logit_scale_init
    section: model
    label: 'Model: Structural logit scale init'
    description: Initial gamma multiplier for normalized structural logits.
    affects: Protects observed logits from structural over-dominance at training start.
    default: 0.1
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: ClassAwareStructuralScoring
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1002
  model.structural_logit_scale_max:
    path: model.structural_logit_scale_max
    section: model
    label: 'Model: Structural logit scale max'
    description: Upper clamp for learned structural logit gamma.
    affects: Prevents the structural branch from dominating observed logits.
    default: 2.0
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: ClassAwareStructuralScoring
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1003
  model.structural_observed_scale_min:
    path: model.structural_observed_scale_min
    section: model
    label: 'Model: Structural observed scale min'
    description: Lower clamp for detached observed-logit magnitude used to scale structural logits.
    affects: Keeps normalized structural logits numerically meaningful when observed logits are small.
    default: 1.0
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: ClassAwareStructuralScoring
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1004
  model.structural_observed_scale_max:
    path: model.structural_observed_scale_max
    section: model
    label: 'Model: Structural observed scale max'
    description: Upper clamp for detached observed-logit magnitude used to scale structural logits.
    affects: Keeps structural-logit scale aligned without inheriting extreme observed-logit magnitudes.
    default: 10.0
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: ClassAwareStructuralScoring
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1005
  model.structural_stats_beta:
    path: model.structural_stats_beta
    section: model
    label: 'Model: Structural stats beta'
    description: Fixed beta multiplier for additive stats projection in structural node features.
    affects: Controls how strongly struct_x statistics enrich structural identity embeddings before structural GNN message passing.
    default: 0.1
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: ClassAwareStructuralScoring
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1006
  model.topology_state_beta:
    path: model.topology_state_beta
    section: model
    label: 'Model: Topology-state beta'
    description: Scales gated observed prefix-state projection before topology message passing in TopologyStateEncoder and TopologyStateGraphEncoder.
    affects: Strength of early observed-state injection into EOPKG topology.
    default: 0.5
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1007
  model.topology_state_beta_max:
    path: model.topology_state_beta_max
    section: model
    label: 'Model: Topology-state beta max'
    description: Forward-time upper clamp for topology_state_beta.
    affects: Prevents over-amplification of prefix-state injection.
    default: 2.0
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1008
  model.topology_state_gate_init_bias:
    path: model.topology_state_gate_init_bias
    section: model
    label: 'Model: Topology-state gate init bias'
    description: Initial bias for sigmoid gate over prefix-state projection.
    affects: Controls whether early topology-state fusion starts conservative or aggressive.
    default: -2.0
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1009
  model.topology_state_class_pooling:
    path: model.topology_state_class_pooling
    section: model
    label: 'Model: Topology-state class pooling'
    description: Node-to-class pooling for TopologyStateEncoder; logmeanexp is cardinality-normalized and is the default.
    affects: Duplicate activity cardinality bias and calibration.
    default: logmeanexp
    enum:
    - logmeanexp
    - mean
    - max
    - logsumexp
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: TopologyStateEncoder
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1010
  model.topology_state_dropout:
    path: model.topology_state_dropout
    section: model
    label: 'Model: Topology-state dropout'
    description: Dropout applied to prefix-state projection in TopologyStateEncoder and TopologyStateGraphEncoder.
    affects: Regularization of early topology-state fusion.
    default: 0.2
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1011
  model.topology_graph_pooling:
    path: model.topology_graph_pooling
    section: model
    label: 'Model: Topology graph pooling'
    description: Graph-level pooling strategy for TopologyStateGraphEncoder; mean mirrors etalon/article-style global_mean_pool.
    affects: How structural node states become one graph-level topology context before classification.
    default: mean
    enum:
    - mean
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: TopologyStateGraphEncoder
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1012
  model.structural_prior_pooling:
    path: model.structural_prior_pooling
    section: model
    label: 'Model: Structural prior pooling'
    description: Pooling strategy for structural node embeddings in StructuralPriorEncoder; mean mirrors etalon global_mean_pool.
    affects: How BPMN/EOPKG node states become one structural context vector before classifier fusion.
    default: mean
    enum:
    - mean
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructuralPriorEncoder
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1013
  model.structural_prior_fusion:
    path: model.structural_prior_fusion
    section: model
    label: 'Model: Structural prior fusion'
    description: Fusion operator for observed context and pooled structural context in StructuralPriorEncoder.
    affects: concat is the direct etalon-like baseline; gated_concat learns how strongly to pass structural context.
    default: concat
    enum:
    - concat
    - gated_concat
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructuralPriorEncoder
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1014
  model.structural_prior_gate_init_bias:
    path: model.structural_prior_gate_init_bias
    section: model
    label: 'Model: Structural prior gate init bias'
    description: Initial sigmoid gate bias for gated_concat; negative values start with conservative structural influence.
    affects: Initial strength of structural context in gated StructuralPriorEncoder.
    default: -1.0
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructuralPriorEncoder
      model.structural_prior_fusion: gated_concat
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1015
  model.struct_xattn_layers:
    path: model.struct_xattn_layers
    section: model
    label: 'Model: StructXAttn layers'
    description: Injection placement for token-level structural cross-attention.
    affects: post_conv2 is safer; after_each_conv makes observed message passing structurally guided.
    default: post_conv2
    enum:
    - post_conv2
    - after_each_conv
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1016
  model.struct_xattn_heads:
    path: model.struct_xattn_heads
    section: model
    label: 'Model: StructXAttn heads'
    description: Number of heads for token-level structural cross-attention.
    affects: Capacity and compute cost of observed-node to structure-node attention.
    default: 4
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1017
  model.struct_xattn_dropout:
    path: model.struct_xattn_dropout
    section: model
    label: 'Model: StructXAttn dropout'
    description: Dropout used inside and after StructXAttn.
    affects: Regularization of structural token guidance.
    default: 0.2
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1018
  model.struct_xattn_scale_init:
    path: model.struct_xattn_scale_init
    section: model
    label: 'Model: StructXAttn scale init'
    description: Initial gamma multiplier for structural cross-attention delta.
    affects: Conservative start for structural guidance.
    default: 0.1
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1019
  model.struct_xattn_scale_max:
    path: model.struct_xattn_scale_max
    section: model
    label: 'Model: StructXAttn scale max'
    description: Upper clamp for learned StructXAttn gamma.
    affects: Prevents structural attention delta from dominating observed node states.
    default: 2.0
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1020
  model.struct_xattn_use_layer_norm:
    path: model.struct_xattn_use_layer_norm
    section: model
    label: 'Model: StructXAttn layer norm'
    description: Apply LayerNorm after residual structural cross-attention update.
    affects: Stabilizes token-level structural updates.
    default: 'true'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1021
  model.struct_xattn_use_gate:
    path: model.struct_xattn_use_gate
    section: model
    label: 'Model: StructXAttn gate'
    description: Use a sigmoid gate over the structural attention delta.
    affects: Lets observed nodes suppress noisy structural guidance.
    default: 'true'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1022
  model.struct_xattn_gate_init_bias:
    path: model.struct_xattn_gate_init_bias
    section: model
    label: 'Model: StructXAttn gate init bias'
    description: Initial sigmoid gate bias for structural attention delta.
    affects: Negative values start StructXAttn conservatively.
    default: -2.0
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
      model.struct_xattn_use_gate: 'true'
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1023
  model.struct_xattn_merge_mode:
    path: model.struct_xattn_merge_mode
    section: model
    label: 'Model: StructXAttn merge mode'
    description: Residual merge semantics for token-level structural cross-attention.
    affects: Controls whether StructXAttn uses legacy post-merge LayerNorm or stable pre-normalized structural context.
    default: post_norm_residual
    enum:
    - post_norm_residual
    - pre_norm_context
    - residual_only
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1024
  model.struct_xattn_delta_ratio_max:
    path: model.struct_xattn_delta_ratio_max
    section: model
    label: 'Model: StructXAttn delta ratio max'
    description: Optional cap for mean_abs(delta) divided by mean_abs(observed) in StructXAttn residual merge.
    affects: Prevents structural cross-attention delta from overpowering observed node states during drift.
    default: ''
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
      model.fusion_mode: StructXAttn
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1025
  model.observed_encoder:
    path: model.observed_encoder
    section: model
    label: 'Model: observed encoder'
    description: Observed-prefix encoder used by EOPKGTopologyConditioned.
    affects: Defines how the prefix graph state is encoded before candidate scoring.
    default: GATv2
    enum:
    - GATv2
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1030
      readonly: true
  model.struct_encoder:
    path: model.struct_encoder
    section: model
    label: 'Model: topology candidate encoder'
    description: Structural encoder used to produce topology candidate prototypes.
    affects: Defines message passing over topology candidates before scoring.
    default: GATv2
    enum:
    - GATv2
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1031
      readonly: true
  model.candidate_scoring:
    path: model.candidate_scoring
    section: model
    label: 'Model: candidate scoring'
    description: Scoring function between observed prefix state and topology candidate prototypes.
    affects: Controls candidate ranking geometry for EOPKGTopologyConditioned.
    default: cosine
    enum:
    - cosine
    - bilinear
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: model
      group: core
      priority: 20
      order: 32
  model.candidate_pooling:
    path: model.candidate_pooling
    section: model
    label: 'Model: duplicate candidate pooling'
    description: Aggregation policy from topology node scores to label-level logits in Stage 1.
    affects: Controls duplicate activity handling in topology-conditioned candidate scoring.
    default: logmeanexp
    enum:
    - logmeanexp
    - mean
    - max
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: model
      group: core
      priority: 20
      order: 33
  model.candidate_temperature_init:
    path: model.candidate_temperature_init
    section: model
    label: 'Model: candidate temperature'
    description: Fixed or initial temperature for topology candidate cosine scores.
    affects: Controls sharpness of candidate-score logits and MIL pooling gradients.
    default: 0.1
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1032
  model.candidate_temperature_min:
    path: model.candidate_temperature_min
    section: model
    label: 'Model: candidate temperature min'
    description: Lower clamp for trainable candidate temperature.
    affects: Prevents candidate scores from becoming too sharp when temperature is trainable.
    default: 0.05
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1033
  model.candidate_temperature_max:
    path: model.candidate_temperature_max
    section: model
    label: 'Model: candidate temperature max'
    description: Upper clamp for trainable candidate temperature.
    affects: Prevents candidate scores from becoming too flat when temperature is trainable.
    default: 10.0
    enum: []
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1034
  model.candidate_temperature_trainable:
    path: model.candidate_temperature_trainable
    section: model
    label: 'Model: train candidate temperature'
    description: Allows candidate temperature to be optimized; canonical Stage 1 keeps it fixed.
    affects: Useful for ablation, but may destabilize candidate ranking if enabled.
    default: false
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1035
  model.dropout:
    path: model.dropout
    section: model
    label: 'Модель: Dropout'
    description: Currently only sequential_prefix is implemented; prefix events are represented as a sequential event graph.
    affects: Prefix graph construction strategy for model input.
    default: sequential_prefix
    enum:
    - sequential_prefix
    required_in_modes: []
    required_when: {}
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
  model.graph_strategy:
    path: model.graph_strategy
    section: model
    label: 'Модель: Graph стратегія'
    description: Model architecture/hyperparameter.
    affects: Capacity, compute footprint, and generalization behavior.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
  model.hidden_dim:
    path: model.hidden_dim
    section: model
    label: 'Модель: Прихований розмірність'
    description: Model architecture/hyperparameter.
    affects: Capacity, compute footprint, and generalization behavior.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
  model.model_label:
    path: model.model_label
    section: model
    label: 'Модель: Модель мітка'
    description: Model architecture/hyperparameter.
    affects: Capacity, compute footprint, and generalization behavior.
    default: ''
    enum:
    - GATv2
    - GCN
    required_in_modes: []
    required_when: {}
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
  model.pooling_strategy:
    path: model.pooling_strategy
    section: model
    label: 'Модель: Пулінг стратегія'
    description: Model architecture/hyperparameter.
    affects: Capacity, compute footprint, and generalization behavior.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
  model.struct_encoder_type:
    path: model.struct_encoder_type
    section: model
    label: 'Модель: Struct encoder type'
    description: Encoder type for structural branch of EOPKGGATv2.
    affects: Structure branch message-passing behavior in EOPKG mode.
    default: GATv2Conv
    enum:
    - GATv2Conv
    - GCNConv
    required_in_modes: []
    required_when:
      model.type: EOPKGGATv2
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
      readonly: true
  model.struct_hidden_dim:
    path: model.struct_hidden_dim
    section: model
    label: 'Модель: Struct прихований розмірність'
    description: global_mean pools all prefix node embeddings; last_node uses the final prefix node embedding as the process state.
    affects: How observed prefix node embeddings are pooled before classification.
    default: global_mean
    enum:
    - global_mean
    - last_node
    required_in_modes: []
    required_when: {}
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
  model.type:
    path: model.type
    section: model
    label: 'Модель: Type'
    description: Main model type (baseline or EOPKG).
    affects: Core runtime architecture and availability of structure/statistics branch.
    default: BaselineGATv2
    enum:
    - BaselineGATv2
    - BaselineGCN
    - EOPKGGATv2
    - EOPKGTopologyConditioned
    required_in_modes: []
    required_when: {}
    ui:
      tab: model
      group: advanced
      priority: 100
      order: 1000
  seed:
    path: seed
    section: seed
    label: Фіксоване зерно (seed)
    description: Global random seed for reproducible runs.
    affects: Reproducibility of stochastic components.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  sync_stats.confidence_weights.coverage:
    path: sync_stats.confidence_weights.coverage
    section: sync_stats
    label: 'Синхронізація статистики: Coverage'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.confidence_weights.freshness:
    path: sync_stats.confidence_weights.freshness
    section: sync_stats
    label: 'Синхронізація статистики: Freshness'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.confidence_weights.sample_size:
    path: sync_stats.confidence_weights.sample_size
    section: sync_stats
    label: 'Синхронізація статистики: Sample size'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.enabled:
    path: sync_stats.enabled
    section: sync_stats
    label: 'Синхронізація статистики: Увімкнено'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.freshness_half_life_days:
    path: sync_stats.freshness_half_life_days
    section: sync_stats
    label: 'Синхронізація статистики: Freshness half life days'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.process_filters:
    path: sync_stats.process_filters
    section: sync_stats
    label: 'Синхронізація статистики: Процес фільтри'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.process_scope_policy:
    path: sync_stats.process_scope_policy
    section: sync_stats
    label: 'Синхронізація статистики: Процес scope політика'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.show_progress:
    path: sync_stats.show_progress
    section: sync_stats
    label: 'Синхронізація статистики: Show progress'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.stats_time_policy:
    path: sync_stats.stats_time_policy
    section: sync_stats
    label: 'Синхронізація статистики: Статистика time політика'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.tenant_filters:
    path: sync_stats.tenant_filters
    section: sync_stats
    label: 'Синхронізація статистики: Тенант фільтри'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.windows_days:
    path: sync_stats.windows_days
    section: sync_stats
    label: 'Синхронізація статистики: Windows days'
    description: sync-stats snapshot generation parameter.
    affects: Quality and temporal relevance of stats snapshots.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: input
      group: advanced
      priority: 100
      order: 1000
  sync_stats.quality_gate.enabled:
    path: sync_stats.quality_gate.enabled
    section: sync_stats
    label: Producer quality gate enabled
    description: Enables producer-side quality gate in sync-stats.
    affects: Whether low-quality snapshots are flagged/skipped during sync.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 10
  sync_stats.quality_gate.on_fail:
    path: sync_stats.quality_gate.on_fail
    section: sync_stats
    label: Producer quality gate on_fail
    description: Action when producer quality thresholds are not met.
    affects: Snapshot persistence policy in sync-stats.
    default: write_with_flag
    enum:
    - write_with_flag
    - skip_snapshot
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 11
  sync_stats.quality_gate.warn_on_fail:
    path: sync_stats.quality_gate.warn_on_fail
    section: sync_stats
    label: Producer quality gate warn_on_fail
    description: Emit warning when producer quality gate fails.
    affects: Sync diagnostics verbosity.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 12
  sync_stats.quality_gate.zero_dominant_threshold:
    path: sync_stats.quality_gate.zero_dominant_threshold
    section: sync_stats
    label: Producer zero-dominant threshold
    description: Threshold above which snapshot is considered zero-dominant.
    affects: Producer quality gate acceptance.
    default: 0.95
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 13
  sync_stats.quality_gate.min_non_zero_ratio_overall:
    path: sync_stats.quality_gate.min_non_zero_ratio_overall
    section: sync_stats
    label: Producer min non-zero ratio overall
    description: Minimal overall non-zero ratio required by producer gate.
    affects: Producer quality gate acceptance.
    default: 0.0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 14
  sync_stats.quality_gate.min_history_coverage_percent:
    path: sync_stats.quality_gate.min_history_coverage_percent
    section: sync_stats
    label: Producer min history coverage percent
    description: Minimal history coverage required by producer gate.
    affects: Producer quality gate acceptance.
    default: 0.0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 15
  sync_stats.alignment_gate.enabled:
    path: sync_stats.alignment_gate.enabled
    section: sync_stats
    label: Producer alignment gate enabled
    description: Enables producer-side activity-to-topology alignment gate in sync-stats.
    affects: Whether weakly aligned stats snapshots are flagged/skipped/raised during sync.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 20
  sync_stats.alignment_gate.profile:
    path: sync_stats.alignment_gate.profile
    section: sync_stats
    label: Producer alignment profile
    description: Matching profile for activity-to-topology alignment.
    affects: Alignment denominator, candidate matching strategy, and research strictness.
    default: legacy_exact
    enum:
    - legacy_exact
    - safe_normalized
    - research_strict
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 21
  sync_stats.alignment_gate.candidate_node_fields:
    path: sync_stats.alignment_gate.candidate_node_fields
    section: sync_stats
    label: Producer alignment candidate fields
    description: Ordered topology node fields used as activity mapping candidates.
    affects: Which topology identifiers/names can satisfy the alignment gate.
    default:
    - id
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 22
  sync_stats.alignment_gate.ignore_structural_only_nodes:
    path: sync_stats.alignment_gate.ignore_structural_only_nodes
    section: sync_stats
    label: Producer alignment ignore structural nodes
    description: Exclude structural-only BPMN nodes from node coverage denominator.
    affects: Node coverage semantics for BPMN processes with events and gateways.
    default: false
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 23
  sync_stats.alignment_gate.strip_classifier_suffix:
    path: sync_stats.alignment_gate.strip_classifier_suffix
    section: sync_stats
    label: Producer alignment strip classifier suffix
    description: Strip lifecycle classifier suffixes such as +complete or ::complete before matching.
    affects: Activity mapping when logs include classifier-extended names.
    default: false
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 24
  sync_stats.alignment_gate.normalize_case:
    path: sync_stats.alignment_gate.normalize_case
    section: sync_stats
    label: Producer alignment normalize case
    description: Use case-insensitive candidate matching after exact matching.
    affects: Activity mapping tolerance for casing differences.
    default: false
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 25
  sync_stats.alignment_gate.collapse_separators:
    path: sync_stats.alignment_gate.collapse_separators
    section: sync_stats
    label: Producer alignment collapse separators
    description: Collapse spaces, underscores, and hyphens during normalized matching.
    affects: Activity mapping tolerance for separator differences.
    default: false
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 26
  sync_stats.alignment_gate.fail_on_ambiguity:
    path: sync_stats.alignment_gate.fail_on_ambiguity
    section: sync_stats
    label: Producer alignment fail on ambiguity
    description: Reject snapshots when an event activity can map to multiple topology nodes.
    affects: False-positive prevention for normalized activity matching.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 27
  sync_stats.alignment_gate.warn_on_fail:
    path: sync_stats.alignment_gate.warn_on_fail
    section: sync_stats
    label: Producer alignment gate warn_on_fail
    description: Emit warning when producer alignment gate fails.
    affects: Sync diagnostics verbosity for activity-to-topology mismatches.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 28
  sync_stats.alignment_gate.min_event_match_ratio:
    path: sync_stats.alignment_gate.min_event_match_ratio
    section: sync_stats
    label: Producer min event match ratio
    description: Minimal ratio of runtime events whose activity ids match topology nodes.
    affects: Activity-to-topology alignment acceptance for stats snapshots.
    default: 0.6
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 29
  sync_stats.alignment_gate.min_unique_activity_coverage:
    path: sync_stats.alignment_gate.min_unique_activity_coverage
    section: sync_stats
    label: Producer min unique activity coverage
    description: Minimal coverage of unique runtime activities by topology nodes.
    affects: Activity-to-topology alignment acceptance for stats snapshots.
    default: 0.6
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 30
  sync_stats.alignment_gate.min_node_coverage:
    path: sync_stats.alignment_gate.min_node_coverage
    section: sync_stats
    label: Producer min node coverage
    description: Minimal topology node coverage by matched runtime activities.
    affects: Activity-to-topology alignment acceptance for stats snapshots.
    default: 0.0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 31
  sync_stats.alignment_gate.on_fail:
    path: sync_stats.alignment_gate.on_fail
    section: sync_stats
    label: Producer alignment gate on_fail
    description: Action when producer alignment thresholds are not met.
    affects: Snapshot persistence/fail-fast policy in sync-stats.
    default: write_with_flag
    enum:
    - write_with_flag
    - skip_snapshot
    - raise
    required_in_modes: []
    required_when: {}
    ui:
      tab: eopkg
      group: sync_stats
      priority: 2
      order: 32
  tracking.backend:
    path: tracking.backend
    section: tracking
    label: 'Трекінг: Бекенд'
    description: Tracking backend. Current supported value in this UI is mlflow.
    affects: Run observability and experiment comparison.
    default: mlflow
    enum:
    - mlflow
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  tracking.enabled:
    path: tracking.enabled
    section: tracking
    label: 'Трекінг: Увімкнено'
    description: Experiment tracking parameter.
    affects: Run observability and experiment comparison.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  tracking.tracing.enabled:
    path: tracking.tracing.enabled
    section: tracking
    label: 'Tracing: enabled'
    description: Enable selective structural prediction traces for qualitative debugging.
    affects: MLflow span recording or JSONL fallback during test/eval_drift inference.
    default: false
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1010
  tracking.tracing.stages:
    path: tracking.tracing.stages
    section: tracking
    label: 'Tracing: stages'
    description: Comma-separated stages where selective traces may be recorded.
    affects: Limits trace collection to test, eval_drift_one_pass, or other supported stages.
    default: test,eval_drift_one_pass
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1011
  tracking.tracing.sample_policy:
    path: tracking.tracing.sample_policy
    section: tracking
    label: 'Tracing: sample policy'
    description: Selects which predictions become traces.
    affects: Trace volume and diagnostic focus.
    default: interesting
    enum:
    - interesting
    - random
    - all
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1012
  tracking.tracing.max_traces_per_run:
    path: tracking.tracing.max_traces_per_run
    section: tracking
    label: 'Tracing: max per run'
    description: Hard limit for recorded trace events in one run.
    affects: Runtime overhead and MLflow/JSONL artifact size.
    default: 80
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1013
  tracking.tracing.max_traces_per_stage:
    path: tracking.tracing.max_traces_per_stage
    section: tracking
    label: 'Tracing: max per stage'
    description: Per-stage limit for recorded trace events.
    affects: Balance of traces across test and drift evaluation stages.
    default: 40
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1014
  tracking.tracing.max_traces_per_version:
    path: tracking.tracing.max_traces_per_version
    section: tracking
    label: 'Tracing: max per version'
    description: Per process-version limit for recorded trace events.
    affects: Prevents one version from consuming the whole trace budget.
    default: 10
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1015
  tracking.tracing.random_sample_rate:
    path: tracking.tracing.random_sample_rate
    section: tracking
    label: 'Tracing: random sample rate'
    description: Probability for random trace sampling when sample_policy=random.
    affects: Trace volume under random sampling.
    default: 0.0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1016
  tracking.tracing.high_confidence_threshold:
    path: tracking.tracing.high_confidence_threshold
    section: tracking
    label: 'Tracing: high confidence threshold'
    description: Confidence threshold for high-confidence error traces.
    affects: Which wrong predictions are treated as high-confidence diagnostics.
    default: 0.8
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1017
  tracking.tracing.low_confidence_threshold:
    path: tracking.tracing.low_confidence_threshold
    section: tracking
    label: 'Tracing: low confidence threshold'
    description: Confidence threshold for low-confidence correct traces.
    affects: Which correct predictions are traced as unstable-but-right cases.
    default: 0.4
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1018
  tracking.tracing.top_k:
    path: tracking.tracing.top_k
    section: tracking
    label: 'Tracing: top-k classes'
    description: Number of top predicted classes included in trace payload.
    affects: Trace payload size and prediction explanation detail.
    default: 5
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1019
  tracking.tracing.log_json_artifact_fallback:
    path: tracking.tracing.log_json_artifact_fallback
    section: tracking
    label: 'Tracing: JSONL fallback'
    description: Write trace events to a rank/PID-scoped JSONL fallback and log it as an artifact.
    affects: Trace availability when MLflow tracing spans are unavailable.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1020
  tracking.tracing.allow_nonzero_rank_fallback:
    path: tracking.tracing.allow_nonzero_rank_fallback
    section: tracking
    label: 'Tracing: allow nonzero rank fallback'
    description: Allow nonzero distributed ranks to write separate JSONL fallback files.
    affects: DDP safety for fallback trace files.
    default: false
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1021
  tracking.tags.model_family:
    path: tracking.tags.model_family
    section: tracking
    label: 'Трекінг: Модель family'
    description: Experiment tracking parameter.
    affects: Run observability and experiment comparison.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  tracking.tags.stage:
    path: tracking.tags.stage
    section: tracking
    label: 'Трекінг: Stage'
    description: Experiment tracking parameter.
    affects: Run observability and experiment comparison.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  tracking.uri:
    path: tracking.uri
    section: tracking
    label: 'Трекінг: Uri'
    description: Experiment tracking parameter.
    affects: Run observability and experiment comparison.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.batch_size:
    path: training.batch_size
    section: training
    label: 'Тренування: Batch size'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.class_weight_cap:
    path: training.class_weight_cap
    section: training
    label: 'Тренування: Class weight cap'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.dataloader_num_workers:
    path: training.dataloader_num_workers
    section: training
    label: 'Тренування: DataLoader workers'
    description: Кількість паралельних воркерів для підготовки batch під час train/val/test.
      0 = без мультипроцесності.
    affects: Використання CPU та швидкість ітерації по batches.
    default: 0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 90
      order: 1000
  training.dataloader_persistent_workers:
    path: training.dataloader_persistent_workers
    section: training
    label: 'Тренування: DataLoader persistent workers'
    description: Якщо true, воркери не перезапускаються між епохами (працює лише коли
      workers > 0).
    affects: Зменшує накладні витрати старту епох при довгих тренуваннях.
    default: true
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.dataloader_pin_memory:
    path: training.dataloader_pin_memory
    section: training
    label: 'Тренування: DataLoader pin memory'
    description: Якщо true, батчі фіксуються в pinned RAM для швидшої передачі на
      GPU.
    affects: Швидкість input pipeline при CUDA; на CPU режим майже не впливає.
    default: false
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.dataloader_prefetch_factor:
    path: training.dataloader_prefetch_factor
    section: training
    label: 'Тренування: DataLoader prefetch factor'
    description: Скільки майбутніх батчів наперед готує кожен воркер (працює лише
      коли workers > 0).
    affects: Баланс між пропускною здатністю pipeline та споживанням RAM.
    default: 2
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.delta:
    path: training.delta
    section: training
    label: 'Тренування: Delta'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.device:
    path: training.device
    section: training
    label: 'Тренування: Device'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.ece_bins:
    path: training.ece_bins
    section: training
    label: 'Тренування: Ece bins'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.epochs:
    path: training.epochs
    section: training
    label: 'Тренування: Epochs'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.learning_rate:
    path: training.learning_rate
    section: training
    label: 'Тренування: Learning rate'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.loss_function:
    path: training.loss_function
    section: training
    label: 'Тренування: Loss function'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.learning_strategy:
    path: training.learning_strategy
    section: training
    label: 'Training: Learning strategy'
    description: Trainer-level methodology. standard preserves current behavior; topology_conditioned adds train-time topology dependency objectives.
    affects: Separates model fusion mode from the methodology used to train topology dependence.
    default: standard
    enum:
    - standard
    - topology_conditioned
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1013
  training.dynamic_candidate_contract_enabled:
    path: training.dynamic_candidate_contract_enabled
    section: training
    label: 'Training: Dynamic candidate contract'
    description: Use model.forward_candidate() for EOPKGTopologyConditioned and train/evaluate through topology-local candidate logits projected back to fixed labels for compatibility.
    affects: Enables Stage 2 candidate-scoring loss path while preserving existing trainer/evaluator metrics.
    default: 'false'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1014
  training.candidate_contract_mode:
    path: training.candidate_contract_mode
    section: training
    label: 'Training: Candidate contract mode'
    description: Prediction/loss contract. fixed_label uses the legacy fixed activity head; fixed_projection uses dynamic candidates projected back to fixed labels; candidate_id requires topology-homogeneous batches and is reserved for true candidate-level training/evaluation.
    affects: Controls whether topology candidates are evaluated as fixed labels, compatibility projections, or true topology-local candidate ids.
    default: fixed_label
    enum:
    - fixed_label
    - fixed_projection
    - candidate_id
    required_in_modes: []
    required_when:
      model.type: EOPKGTopologyConditioned
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1015
  training.candidate_batch_topology_policy:
    path: training.candidate_batch_topology_policy
    section: training
    label: 'Training: Candidate batch topology policy'
    description: Batch topology policy for candidate_id mode. single_topology_required uses topology-homogeneous batches and fails manually mixed batches; group_by_topology is reserved for a future implementation.
    affects: Prevents silent candidate-axis corruption when a batch contains multiple process versions or stats snapshots.
    default: single_topology_required
    enum:
    - single_topology_required
    - group_by_topology
    required_in_modes: []
    required_when:
      training.candidate_contract_mode: candidate_id
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1016
  training.topology_conditioning_wrong_version_negative_enabled:
    path: training.topology_conditioning_wrong_version_negative_enabled
    section: training
    label: 'Training: TC wrong-version negative'
    description: Enable known wrong-version topology negative loss for topology_conditioned learning.
    affects: Forces the model to prefer the correct known topology over another known version.
    default: 'true'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      training.learning_strategy: topology_conditioned
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1014
  training.topology_conditioning_wrong_version_negative_weight:
    path: training.topology_conditioning_wrong_version_negative_weight
    section: training
    label: 'Training: TC wrong-version weight'
    description: Weight for wrong-version topology negative margin loss.
    affects: Strength of contrastive topology dependency pressure.
    default: 0.1
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_wrong_version_negative_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1015
  training.topology_conditioning_wrong_version_margin:
    path: training.topology_conditioning_wrong_version_margin
    section: training
    label: 'Training: TC wrong-version margin'
    description: Required CE gap between correct topology and wrong known-version topology.
    affects: Minimum pressure for topology-sensitive learning.
    default: 0.2
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_wrong_version_negative_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1016
  training.topology_conditioning_drop_edges_negative_enabled:
    path: training.topology_conditioning_drop_edges_negative_enabled
    section: training
    label: 'Training: TC drop-edges negative'
    description: Enable same-version physically dropped structural edge negative loss.
    affects: Tests whether model predictions react to local topology corruption.
    default: 'true'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      training.learning_strategy: topology_conditioned
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1017
  training.topology_conditioning_drop_edges_negative_weight:
    path: training.topology_conditioning_drop_edges_negative_weight
    section: training
    label: 'Training: TC drop-edges weight'
    description: Weight for same-version dropped-edge topology negative margin loss.
    affects: Strength of local topology corruption pressure.
    default: 0.05
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_drop_edges_negative_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1018
  training.topology_conditioning_drop_edges_margin:
    path: training.topology_conditioning_drop_edges_margin
    section: training
    label: 'Training: TC drop-edges margin'
    description: Required CE gap between correct topology and dropped-edge topology.
    affects: Minimum local corruption sensitivity expected from the model.
    default: 0.1
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_drop_edges_negative_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1019
  training.topology_conditioning_drop_edges_ratio:
    path: training.topology_conditioning_drop_edges_ratio
    section: training
    label: 'Training: TC drop-edges ratio'
    description: Fraction of structural edges physically removed in the negative topology view.
    affects: Severity of topology corruption; high values can break BPM reachability.
    default: 0.15
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_drop_edges_negative_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1020
  training.topology_conditioning_allowed_set_loss_enabled:
    path: training.topology_conditioning_allowed_set_loss_enabled
    section: training
    label: 'Training: TC allowed-set loss'
    description: Enable train-time topology consistency loss over allowed target candidates.
    affects: Adds mask-aware learning pressure independent from mask-guided inference logits.
    default: 'true'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      training.learning_strategy: topology_conditioned
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1021
  training.topology_conditioning_allowed_set_loss_weight:
    path: training.topology_conditioning_allowed_set_loss_weight
    section: training
    label: 'Training: TC allowed-set weight'
    description: Weight for topology allowed-set loss.
    affects: Strength of topology-compatible target-set pressure.
    default: 0.05
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_allowed_set_loss_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1022
  training.topology_conditioning_retention_enabled:
    path: training.topology_conditioning_retention_enabled
    section: training
    label: 'Training: TC retention'
    description: Enable version-weighted CE/rehearsal for already seen versions.
    affects: Controls how strongly older versions are retained while newer versions dominate.
    default: 'true'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      training.learning_strategy: topology_conditioned
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1023
  training.topology_conditioning_retention_policy:
    path: training.topology_conditioning_retention_policy
    section: training
    label: 'Training: TC retention policy'
    description: Policy for per-version retention sample weights.
    affects: Defines how known older process versions contribute to topology_conditioned training.
    default: version_decay
    enum:
    - version_decay
    required_in_modes: []
    required_when:
      training.topology_conditioning_retention_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1024
  training.topology_conditioning_current_version_policy:
    path: training.topology_conditioning_current_version_policy
    section: training
    label: 'Training: TC current version policy'
    description: Rule for identifying the current/latest seen version in retention weighting.
    affects: Determines version distance for retention weights.
    default: latest_in_train
    enum:
    - latest_in_train
    required_in_modes: []
    required_when:
      training.topology_conditioning_retention_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1025
  training.topology_conditioning_retention_recent_versions:
    path: training.topology_conditioning_retention_recent_versions
    section: training
    label: 'Training: TC recent versions'
    description: Number of previous versions treated as recent for retention weighting.
    affects: Width of the rehearsal window for older versions.
    default: 1
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_retention_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1026
  training.topology_conditioning_retention_recent_weight:
    path: training.topology_conditioning_retention_recent_weight
    section: training
    label: 'Training: TC recent weight'
    description: Sample weight for recent previous versions.
    affects: Retention pressure for versions close to the current one.
    default: 0.5
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_retention_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1027
  training.topology_conditioning_retention_old_weight:
    path: training.topology_conditioning_retention_old_weight
    section: training
    label: 'Training: TC old weight'
    description: Sample weight for older seen versions outside the recent window.
    affects: Long-tail retention pressure for stale process versions.
    default: 0.15
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_retention_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1028
  training.topology_conditioning_retention_obsolete_weight:
    path: training.topology_conditioning_retention_obsolete_weight
    section: training
    label: 'Training: TC obsolete weight'
    description: Reserved weight for versions considered obsolete by future policies.
    affects: Future extension point for business lifecycle retention policy.
    default: 0.0
    enum: []
    required_in_modes: []
    required_when:
      training.topology_conditioning_retention_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1029
  training.mask_guided_apply_in_eval:
    path: training.mask_guided_apply_in_eval
    section: training
    label: 'Training: Apply mask-guided in eval'
    description: Apply mask-guided logits policy during eval/eval_drift inference.
    affects: Drift and test metrics sensitivity to impossible transitions.
    default: 'true'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 7
  training.mask_guided_hard_threshold:
    path: training.mask_guided_hard_threshold
    section: training
    label: 'Training: Mask hard threshold'
    description: Use hard masking when estimated target_in_mask_rate is at or above this threshold.
    affects: Tradeoff between strict topology filtering and robustness to mask noise.
    default: 1.0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 8
  training.mask_guided_soft_penalty:
    path: training.mask_guided_soft_penalty
    section: training
    label: 'Training: Mask soft penalty'
    description: Logit penalty applied to disallowed classes in soft mode.
    affects: Ranking pressure away from improbable targets without hard cutoff.
    default: 2.0
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: core
      priority: 1
      order: 9
  training.mask_guided_min_samples_for_hard:
    path: training.mask_guided_min_samples_for_hard
    section: training
    label: 'Training: Min samples for hard'
    description: Minimum observed samples before enabling hard masking from reliability estimate.
    affects: Stability of auto-switch between hard and soft masking.
    default: 1
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.structural_aux_loss_enabled:
    path: training.structural_aux_loss_enabled
    section: training
    label: 'Training: Structural auxiliary loss enabled'
    description: Enable auxiliary loss on class-aware structural logits.
    affects: Gives the structural branch a direct training signal instead of relying only on fused logits.
    default: 'false'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      model.fusion_mode: ClassAwareStructuralScoring
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1001
  training.structural_aux_loss_weight:
    path: training.structural_aux_loss_weight
    section: training
    label: 'Training: Structural auxiliary set loss weight'
    description: Weight for set-aware CE over allowed target candidates using structural logits.
    affects: Strength of topology-compatible structural learning signal.
    default: 0.05
    enum: []
    required_in_modes: []
    required_when:
      training.structural_aux_loss_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1002
  training.structural_aux_exact_loss_weight:
    path: training.structural_aux_exact_loss_weight
    section: training
    label: 'Training: Structural auxiliary exact loss weight'
    description: Small exact CE component for structural logits.
    affects: Keeps strict target pressure while set-aware loss handles parallel allowed candidates.
    default: 0.01
    enum: []
    required_in_modes: []
    required_when:
      training.structural_aux_loss_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1003
  training.struct_xattn_contrastive_enabled:
    path: training.struct_xattn_contrastive_enabled
    section: training
    label: 'Training: StructXAttn contrastive enabled'
    description: Enable correct-vs-corrupted topology loss for StructXAttn.
    affects: Forces StructXAttn to prefer correct topology over corrupted topology during training.
    default: 'false'
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when:
      model.fusion_mode: StructXAttn
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1004
  training.struct_xattn_contrastive_weight:
    path: training.struct_xattn_contrastive_weight
    section: training
    label: 'Training: StructXAttn contrastive weight'
    description: Weight for smoothed correct-vs-corrupted topology contrastive loss.
    affects: Strength of explicit structural dependency pressure.
    default: 0.03
    enum: []
    required_in_modes: []
    required_when:
      training.struct_xattn_contrastive_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1005
  training.struct_xattn_contrastive_margin:
    path: training.struct_xattn_contrastive_margin
    section: training
    label: 'Training: StructXAttn contrastive margin'
    description: Required CE advantage of correct topology over corrupted topology.
    affects: Margin in the correct-vs-corrupted topology objective.
    default: 0.05
    enum: []
    required_in_modes: []
    required_when:
      training.struct_xattn_contrastive_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1006
  training.struct_xattn_contrastive_temperature:
    path: training.struct_xattn_contrastive_temperature
    section: training
    label: 'Training: StructXAttn contrastive temperature'
    description: Softplus temperature for smoothed contrastive margin.
    affects: Gradient smoothness of the contrastive topology objective.
    default: 0.1
    enum: []
    required_in_modes: []
    required_when:
      training.struct_xattn_contrastive_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1007
  training.struct_xattn_contrastive_max_loss:
    path: training.struct_xattn_contrastive_max_loss
    section: training
    label: 'Training: StructXAttn contrastive max loss'
    description: Upper clamp for one-batch contrastive loss before weighting.
    affects: Prevents corrupted-topology objective from dominating training.
    default: 0.5
    enum: []
    required_in_modes: []
    required_when:
      training.struct_xattn_contrastive_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1008
  training.struct_xattn_contrastive_warmup_epochs:
    path: training.struct_xattn_contrastive_warmup_epochs
    section: training
    label: 'Training: StructXAttn contrastive warmup'
    description: Number of initial epochs before enabling corrupted-topology forward.
    affects: Lets baseline CE stabilize before structural dependency pressure starts.
    default: 3
    enum: []
    required_in_modes: []
    required_when:
      training.struct_xattn_contrastive_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1009
  training.struct_xattn_corruption_policy:
    path: training.struct_xattn_corruption_policy
    section: training
    label: 'Training: StructXAttn corruption policy'
    description: Tensor-level structural corruption used as the negative topology view.
    affects: Defines the corrupted topology seen by the contrastive objective.
    default: edge_drop
    enum:
    - none
    - edge_drop
    - feature_noise
    - edge_drop_plus_feature_noise
    required_in_modes: []
    required_when:
      training.struct_xattn_contrastive_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1010
  training.struct_xattn_corruption_edge_drop_prob:
    path: training.struct_xattn_corruption_edge_drop_prob
    section: training
    label: 'Training: StructXAttn edge drop'
    description: Probability of dropping each structural edge in the corrupted topology view.
    affects: Strength of edge-level topology corruption.
    default: 0.15
    enum: []
    required_in_modes: []
    required_when:
      training.struct_xattn_contrastive_enabled: 'true'
      training.struct_xattn_corruption_policy: edge_drop
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1011
  training.struct_xattn_corruption_feature_noise_std:
    path: training.struct_xattn_corruption_feature_noise_std
    section: training
    label: 'Training: StructXAttn feature noise'
    description: Gaussian noise std added to struct_x in the corrupted topology view.
    affects: Strength of structural feature corruption.
    default: 0.0
    enum: []
    required_in_modes: []
    required_when:
      training.struct_xattn_contrastive_enabled: 'true'
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1012
  training.patience:
    path: training.patience
    section: training
    label: 'Тренування: Patience'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.retrain:
    path: training.retrain
    section: training
    label: Перенавчати з нуля
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum:
    - 'false'
    - 'true'
    required_in_modes: []
    required_when: {}
    ui:
      tab: legacy
      group: deprecated
      priority: 999
      order: 9999
  training.show_progress:
    path: training.show_progress
    section: training
    label: 'Тренування: Show progress'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.torch_num_interop_threads:
    path: training.torch_num_interop_threads
    section: training
    label: 'Тренування: Torch interop threads'
    description: Кількість inter-op потоків PyTorch для паралелізації між операціями.
      Порожнє значення = системний дефолт.
    affects: Поведінка CPU scheduler при складних графах обчислень.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.torch_num_threads:
    path: training.torch_num_threads
    section: training
    label: 'Тренування: Torch CPU threads'
    description: Обмеження для кількості внутрішніх CPU потоків PyTorch (intra-op).
      Порожнє значення = системний дефолт.
    affects: Завантаження CPU під час обчислень PyTorch.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.tqdm_disable:
    path: training.tqdm_disable
    section: training
    label: 'Тренування: Tqdm disable'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
  training.tqdm_leave:
    path: training.tqdm_leave
    section: training
    label: 'Тренування: Tqdm leave'
    description: Training loop hyperparameter.
    affects: Training stability, speed, and final quality metrics.
    default: ''
    enum: []
    required_in_modes: []
    required_when: {}
    ui:
      tab: general
      group: advanced
      priority: 100
      order: 1000
