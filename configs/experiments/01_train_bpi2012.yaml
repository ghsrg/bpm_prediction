include:
  - "../data/bpi2012_Challenge.yaml"
  - "../model/gatv2_baseline.yaml"

experiment:
  project: "bpm_prediction_mvp1"
  name: "MVP1_Baseline_train_05"
  mode: "train"
  fraction: 1
  split_strategy: "temporal"
  train_ratio: 0.5
  split_ratio: [0.7, 0.2, 0.1]

training:
  retrain: false

seed: 42
