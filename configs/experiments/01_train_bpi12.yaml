include:
  - "../data/bpi2012.yaml"
  - "../model/gatv2_baseline.yaml"

experiment:
  project: "bpm_prediction_mvp1"
  dataset: "bpi2012_loan"
  name: "BPI12_Baseline_Train"
  mode: "train"

training:
  retrain: false
